import type { ChartPoint } from './LineChart';
export function BarChart({ title, data }: { title: string; data: ChartPoint[] }) { const max = Math.max(1, ...data.map((point) => point.value)); return <figure className="chart"><figcaption><span className="chart-key" />{title}</figcaption><div className="bar-chart" role="img" aria-label={title}>{data.map((point) => <div key={point.label}><span title={`${point.label}: ${point.value}`} style={{ height: `${point.value / max * 100}%` }} /><small>{point.label}</small></div>)}</div><table className="sr-only"><caption>{title} data</caption><tbody>{data.map((point) => <tr key={point.label}><th>{point.label}</th><td>{point.value}</td></tr>)}</tbody></table></figure>; }
