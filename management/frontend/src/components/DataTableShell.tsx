import type { ReactNode } from 'react';
export function DataTableShell({ label, controls, children, empty, loading, error, onRetry }: { label: string; controls?: ReactNode; children?: ReactNode; empty?: ReactNode; loading?: boolean; error?: string; onRetry?: () => void }) { return <section className="data-table-shell" aria-label={label}>{controls && <div className="table-controls">{controls}</div>}{loading ? <div className="table-state" aria-live="polite">Loading {label.toLowerCase()}…</div> : error ? <div className="table-state" role="alert"><p>{error}</p>{onRetry && <button onClick={onRetry}>Retry</button>}</div> : children ?? <div className="table-state">{empty ?? 'No results yet.'}</div>}</section>; }
