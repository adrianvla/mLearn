import { ConsoleButton, ConsoleDialog, ConsoleTextField } from './console';
export function PolicyDiffDialog({ open, before, after, warnings=[], summary, onSummaryChange, onCancel, onPublish }: { open:boolean;before:unknown;after:unknown;warnings?:string[];summary:string;onSummaryChange(value:string):void;onCancel():void;onPublish():void }) { return <ConsoleDialog open={open} onOpenChange={(next) => { if (!next) onCancel(); }} title="Publish policy" footer={<><ConsoleButton onClick={onCancel}>Cancel</ConsoleButton><ConsoleButton isDisabled={!summary.trim()} onClick={onPublish}>Publish</ConsoleButton></>}><p>Review the normalized local draft. Inherited hard constraints remain effective.</p>{warnings.map((warning)=><p role="alert" key={warning}>{warning}</p>)}<div><pre aria-label="Current policy">{JSON.stringify(before,null,2)}</pre><pre aria-label="Proposed policy">{JSON.stringify(after,null,2)}</pre></div><ConsoleTextField label="Required summary" value={summary} onChange={onSummaryChange}/></ConsoleDialog>; }
