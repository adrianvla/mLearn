import type { Capability } from '../api/types';
const CAPABILITIES: Capability[] = ['group.view','group.manage','members.view','members.manage','permissions.delegate','policies.view','policies.edit','policies.publish','analytics.view','conversations.view','conversations.export','llm.configure','api_keys.manage'];
export function CapabilityEditor({ value, grantable, onChange }: { value: Capability[]; grantable: readonly Capability[]; onChange(value: Capability[]): void }) { return <fieldset className="capability-editor"><legend>Capabilities</legend>{CAPABILITIES.map((capability) => { const allowed = grantable.includes(capability); return <label key={capability} title={allowed ? undefined : 'You cannot grant a capability you do not hold'}><input aria-label={capability} type="checkbox" checked={value.includes(capability)} disabled={!allowed} onChange={(event) => onChange(event.currentTarget.checked ? [...value, capability] : value.filter((item) => item !== capability))} /><span>{capability}</span>{!allowed && <small>Not grantable</small>}</label>; })}</fieldset>; }
