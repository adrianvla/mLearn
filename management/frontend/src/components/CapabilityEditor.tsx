import type { Capability } from '../api/types';
import { ConsoleSwitch } from './console';
const CAPABILITIES: Capability[] = ['group.view','group.manage','members.view','members.manage','permissions.delegate','policies.view','policies.edit','policies.publish','analytics.view','conversations.view','conversations.export','llm.configure','api_keys.manage'];
export function CapabilityEditor({ value, grantable, onChange }: { value: Capability[]; grantable: readonly Capability[]; onChange(value: Capability[]): void }) { return <fieldset className="capability-editor"><legend>Capabilities</legend>{CAPABILITIES.map((capability) => { const allowed = grantable.includes(capability); return <div key={capability} title={allowed ? undefined : 'You cannot grant a capability you do not hold'}><ConsoleSwitch label={capability} isSelected={value.includes(capability)} isDisabled={!allowed} onChange={(selected) => onChange(selected ? [...value, capability] : value.filter((item) => item !== capability))} />{!allowed && <small>Not grantable</small>}</div>; })}</fieldset>; }
