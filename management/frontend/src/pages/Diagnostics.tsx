import { Link } from 'react-router-dom';import { PageToolbar } from '../components/PageToolbar';import { useAuth } from '../auth/AuthProvider';export default function Diagnostics(){const auth=useAuth();if(auth.status!=='authenticated'||!auth.user.isRoot)return <section className="table-state" role="alert">Diagnostics require the root administrator.</section>;return <div className="resource-page"><PageToolbar title="Diagnostics" description="Root-only operational services, storage, AI status, distribution, and redacted logs."/><div className="diagnostic-links"><Link to="/services">Services</Link><Link to="/storage">Storage</Link><Link to="/ai-status">AI status</Link><Link to="/distribution">Distribution</Link><Link to="/logs">Operational logs</Link></div></div>}
