import { fireEvent,render,screen } from '@testing-library/react';import { expect,it,vi } from 'vitest';import Logs from './Logs';vi.mock('../groups/GroupScopeProvider',()=>({useGroupScope:()=>({status:'ready',selectedGroup:{id:'german-a',name:'German A'},can:()=>true})}));
it('loads scoped conversation summaries and renders decrypted detail response',async()=>{vi.stubGlobal('fetch',vi.fn(async(input:RequestInfo|URL)=>{const url=String(input);const summary={id:'c1',groupId:'german-a',learnerUserId:'learner',status:'completed',createdAt:1700000000,providerId:'openai',modelId:'model',inputTokens:10,outputTokens:5,costMicros:1000,policyVersionId:'policy',policyCompiledHash:'hash',errorCode:null};return json(url.endsWith('/c1')?{summary,messages:[{role:'user',content:'Encrypted at rest, visible after authorization',sequence:0,truncated:false}]}:{items:[summary],nextCursor:null})}));render(<Logs/>);const learner=await screen.findByRole('button',{name:'learner'});fireEvent.click(learner);expect(await screen.findByText('Encrypted at rest, visible after authorization')).toBeVisible();expect(screen.getAllByText('openai / model')).toHaveLength(2);expect(fetch).toHaveBeenCalledWith(expect.stringContaining('groupId=german-a'),expect.anything())});function json(body:unknown){return Promise.resolve(new Response(JSON.stringify(body),{status:200,headers:{'Content-Type':'application/json'}}))}
