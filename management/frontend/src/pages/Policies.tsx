import { useEffect,useMemo,useState } from 'react';import { ApiClient } from '../api/client';import { PageToolbar } from '../components/PageToolbar';import { PolicyDiffDialog } from '../components/PolicyDiffDialog';import { PolicySettingRow } from '../components/PolicySettingRow';import { QuotaEditor,type DraftQuota } from '../components/QuotaEditor';import { useGroupScope } from '../groups/GroupScopeProvider';
const api=new ApiClient();type Json=Record<string,any>;
export default function Policies(){const scope=useGroupScope();const groupId=scope.status==='ready'?scope.selectedGroup?.id:null;const [effective,setEffective]=useState<Json|null>(null);const [draft,setDraft]=useState<Json>({settings:{},features:{},llm:{quotas:[]},governance:{}});const [validation,setValidation]=useState(false);const [error,setError]=useState<string|null>(null);const [diffOpen,setDiffOpen]=useState(false);const [summary,setSummary]=useState('');
useEffect(()=>{if(!groupId)return;const controller=new AbortController();Promise.all([api.get<Json>(`/api/groups/${groupId}/policy/effective`,{signal:controller.signal}),api.get<{document:Json}|null>(`/api/groups/${groupId}/policy/draft`,{signal:controller.signal})]).then(([compiled,local])=>{if(!controller.signal.aborted){setEffective(compiled);setDraft(local?.document??{settings:{},features:{},llm:{quotas:[]},governance:{}})}}).catch((caught)=>setError(caught instanceof Error?caught.message:'Policy could not be loaded'));return()=>controller.abort()},[groupId]);const document=effective?.document??effective;const ancestry=useMemo(()=>new Map<string,string>((document?.ancestry??[]).map((item:any)=>[item.id,item.name])),[document]);const editable=scope.status==='ready'&&scope.can('policies.edit');const publishable=scope.status==='ready'&&scope.can('policies.publish');
const save=async()=>{if(!groupId)return;await api.get(`/api/groups/${groupId}/policy/draft`,{method:'PUT',body:JSON.stringify(draft)});const result=await api.get<{valid:boolean}>(`/api/groups/${groupId}/policy/validate`,{method:'POST'});setValidation(result.valid)};const publish=async()=>{if(!groupId)return;await api.get(`/api/groups/${groupId}/policy/publish`,{method:'POST',body:JSON.stringify({summary})});setDiffOpen(false);setSummary('');setValidation(false)};
return <div className="resource-page"><PageToolbar title="Policies" description="Local rules inherit root-to-leaf and cannot weaken hard ancestor controls." actions={<><button className="secondary-action" disabled={!editable} onClick={()=>void save()}>Save draft</button><button className="primary-action" disabled={!publishable||!validation} onClick={()=>setDiffOpen(true)}>Publish</button></>}/>{error&&<p role="alert">{error}</p>}{document&&<div className="policy-editor"><section><h2>App settings</h2>{Object.entries(document.settings??{}).map(([key,rule]:[string,any])=><PolicySettingRow key={key} label={key} source={rule.sourceGroupId===groupId?undefined:ancestry.get(rule.sourceGroupId)??rule.sourceGroupName} constrained={rule.locked} disabled={rule.locked&&rule.sourceGroupId!==groupId}><input value={String(draft.settings?.[key]?.value??rule.value)} onChange={(event)=>setDraft({...draft,settings:{...draft.settings,[key]:{value:event.currentTarget.value,locked:rule.locked}}})}/></PolicySettingRow>)}</section><section><h2>Features and AI routing</h2>{Object.entries(document.features??{}).map(([key,rule]:[string,any])=><PolicySettingRow key={key} label={key} source={rule.sourceGroupId===groupId?undefined:ancestry.get(rule.sourceGroupId)} constrained={rule.hard} disabled={rule.hard&&rule.sourceGroupId!==groupId}><input type="checkbox" checked={draft.features?.[key]?.enabled??rule.enabled} onChange={(event)=>setDraft({...draft,features:{...draft.features,[key]:{enabled:event.currentTarget.checked,hard:false}}})}/></PolicySettingRow>)}</section><section><h2>Retention and exports</h2><PolicySettingRow label="Activity retention"><input type="number" min="1" max={document.governance.activityRetentionDays} value={draft.governance?.activityRetentionDays??document.governance.activityRetentionDays} onChange={(event)=>setDraft({...draft,governance:{...draft.governance,activityRetentionDays:event.currentTarget.valueAsNumber}})}/></PolicySettingRow></section><section><QuotaEditor quotas={(draft.llm?.quotas??[]) as DraftQuota[]} ancestor={document.llm.quotas} disabled={!editable} onChange={(quotas)=>setDraft({...draft,llm:{...draft.llm,quotas}})}/></section></div>}<PolicyDiffDialog open={diffOpen} before={effective?.document??{}} after={draft} warnings={validation?[]:['Validate the draft before publishing.']} summary={summary} onSummaryChange={setSummary} onCancel={()=>setDiffOpen(false)} onPublish={()=>void publish()}/></div>}
