import { Link } from 'react-router-dom';import { PageToolbar } from '../components/PageToolbar';import { useAuth } from '../auth/AuthProvider';import Config from './Config';export default function Settings(){const auth=useAuth();return <div className="resource-page"><PageToolbar title="Settings" description="School identity, deployment endpoints, retention, security, and backup guidance." actions={auth.status==='authenticated'&&auth.user.isRoot?<Link className="secondary-action" to="/settings/diagnostics">Open Diagnostics</Link>:undefined}/><section className="settings-note"><h2>School administration</h2><p>Timezone, term calendars, retention, and export permissions are governed through Policies and LLM quota calendars. Deployment paths below are redacted and read-only.</p></section><Config/></div>}
