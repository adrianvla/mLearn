import { render,screen } from '@testing-library/react';import { MemoryRouter } from 'react-router-dom';import { expect,it,vi } from 'vitest';import Settings from './Settings';vi.mock('../auth/AuthProvider',()=>({useAuth:()=>({status:'authenticated',user:{isRoot:false}})}));vi.mock('./Config',()=>({default:()=> <div>Redacted deployment configuration</div>}));it('keeps diagnostics hidden from non-root settings',()=>{render(<MemoryRouter><Settings/></MemoryRouter>);expect(screen.getByText('Redacted deployment configuration')).toBeVisible();expect(screen.queryByRole('link',{name:'Open Diagnostics'})).not.toBeInTheDocument()});
