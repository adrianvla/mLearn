import { useEffect,useMemo,useState } from 'react';
import { ApiClient } from '../api/client';import type { GroupNode,Membership } from '../api/types';import { CapabilityEditor } from '../components/CapabilityEditor';import { GroupTree } from '../components/GroupTree';import { PageToolbar } from '../components/PageToolbar';import { useGroupScope } from '../groups/GroupScopeProvider';
const api=new ApiClient();
export default function Groups(){const scope=useGroupScope();const [groups,setGroups]=useState<GroupNode[]>([]);const [selectedId,setSelectedId]=useState<string|null>(null);const [memberships,setMemberships]=useState<Membership[]>([]);const [tab,setTab]=useState('overview');useEffect(()=>{const controller=new AbortController();api.get<{groups:GroupNode[]}>('/api/groups',{signal:controller.signal}).then((result)=>{if(!controller.signal.aborted){setGroups(result.groups);setSelectedId((current)=>current&&result.groups.some((group)=>group.id===current)?current:result.groups[0]?.id??null)}});return()=>controller.abort()},[]);useEffect(()=>{if(!selectedId){setMemberships([]);return}const controller=new AbortController();api.get<{memberships:Membership[]}>(`/api/groups/${encodeURIComponent(selectedId)}/memberships`,{signal:controller.signal}).then((result)=>{if(!controller.signal.aborted)setMemberships(result.memberships)});return()=>controller.abort()},[selectedId]);const selected=groups.find((group)=>group.id===selectedId)??null;const grantable=useMemo(()=>scope.status==='ready'?scope.selectedGroup?.capabilities??[]:[],[scope]);return <div className="resource-page"><PageToolbar title="Groups" description="The authorized school hierarchy, memberships, and delegated authority."/><div className="group-workspace"><aside><input aria-label="Search groups" placeholder="Search groups"/><GroupTree groups={groups} selectedId={selectedId} onSelect={setSelectedId}/></aside><section className="group-detail">{selected?<><header><div><h2>{selected.name}</h2><p>{selected.slug} · {selected.status}</p></div></header><div className="detail-tabs" role="tablist">{['overview','members','permissions','policy','analytics'].map((name)=><button key={name} role="tab" aria-selected={tab===name} onClick={()=>setTab(name)}>{name}</button>)}</div>{tab==='overview'&&<dl><div><dt>Group ID</dt><dd>{selected.id}</dd></div><div><dt>Parent</dt><dd>{selected.parentId??'School root'}</dd></div></dl>}{tab==='members'&&<div className="table-scroll"><table><thead><tr><th>Member</th><th>Status</th><th>Capabilities</th></tr></thead><tbody>{memberships.map((membership)=><tr key={membership.id}><th>{membership.userId??membership.invitedEmail}</th><td>{membership.status}</td><td>{membership.capabilities.length}</td></tr>)}</tbody></table></div>}{tab==='permissions'&&<><p>Inherited authority is enforced by the backend. You can delegate only capabilities you currently hold.</p><CapabilityEditor value={[]} grantable={grantable} onChange={()=>{}}/></>}</>:<p>Select a group.</p>}</section></div></div>}
