import { useState } from 'react';import { Link } from 'react-router-dom';import { useAuth } from '../auth/AuthProvider';export default function Login(){const auth=useAuth();const [email,setEmail]=useState('');const [password,setPassword]=useState('');return <main className="auth-screen"><form onSubmit={(event)=>{event.preventDefault();void auth.login(email,password)}}><header><h1>Sign in to mLearn</h1><p>School administration console</p></header><label>Email<input type="email" autoComplete="username" value={email} onChange={(event)=>setEmail(event.currentTarget.value)} required/></label><label>Password<input type="password" autoComplete="current-password" value={password} onChange={(event)=>setPassword(event.currentTarget.value)} required/></label>{auth.status==='error'&&<p role="alert">{auth.error.message}</p>}<button type="submit">Sign in</button><Link to="/bootstrap">Set up the first administrator</Link></form></main>}
