Summary of changes — feature/watch-together-rooms

Overview
- Moved smart, context-aware components out of the `common` root into a dedicated folder and unified behavior across the UI.

Key changes
- Components (moved/added):
  - `src/renderer/components/common/Smart/WordStatusPill.tsx`
  - `src/renderer/components/common/Smart/wordStatusPillLogic.ts`
  - `src/renderer/components/common/Smart/EasePill.tsx`
  - `src/renderer/components/common/Smart/EasePill.css`
  - `src/renderer/components/common/Smart/index.ts`
- Logic / utilities:
  - `src/renderer/utils/wordForms.ts` — `getWordFormCandidates()` to prefer canonical forms.
  - `src/renderer/components/subtitle/explainerProviderLabel.ts` — provider-aware explainer "Generated by" label.
- Services:
  - `src/renderer/services/statsService.ts` — alias-aware `getWordStatus()` / `setWordStatus()` and legacy alias cleanup.
  - `src/renderer/services/ankiWordsCache.ts` — `findWordInAnkiCache(words: readonly string[])`.
- Consumers updated to use canonical-aware helpers and smart components:
  - `src/renderer/components/subtitle/WordHover.tsx`
  - `src/renderer/components/sidebar/UnknownWordsSidebar.tsx`
  - `src/renderer/windows/wordDefinition/App.tsx`
  - `src/renderer/windows/main/routes/ReaderRoute.tsx`
  - `src/renderer/windows/main/routes/VideoRoute.tsx`
- Tests added/updated:
  - `src/renderer/components/common/Smart/WordStatusPill.test.ts`
  - `src/renderer/components/subtitle/explainerProviderLabel.test.ts`
  - `src/renderer/services/ankiWordsCache.test.ts`
  - updated `src/renderer/services/statsService.test.ts`
- Locales: updated `Explainer.GeneratedBy` template and provider labels in:
  - `src/root-of-app/locales/lang.en.json`
  - `src/root-of-app/locales/lang.de.json`
  - `src/root-of-app/locales/lang.fr.json`
  - `src/root-of-app/locales/lang.ja.json`
  - `src/root-of-app/locales/lang.ru.json`

What this fixes
- Unified the modal warning flow for tracked words between the sidebar `WordStatus` pill and `WordHover`.
- Ensures kana/reading-only forms (e.g. “なかま”) resolve to the canonical tracked word (e.g. “仲間”) for manual status and Anki matching.
- Extracted and isolated the `EasePill` UI into its own component and CSS file to avoid root-level `common` pollution.

Validation performed
- Ran `npm run typecheck` (no type errors reported).
- Ran focused unit tests for modified components and helpers (Vitest; passed locally in focused runs).

Notes
- Branch: `feature/watch-together-rooms`.
- No runtime UI verification performed here — please run the app and spot-check: hovering `なかま` should show the canonical source in the tooltip and the sidebar pill should open the same tracked-word warning modal the hover uses.

If you want, I can now commit these changes to the repo and push the branch (I will also add a short note file and commit in `mlearn-website`).
