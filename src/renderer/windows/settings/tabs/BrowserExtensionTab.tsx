/**
 * Browser Extension Settings Tab
 */

import { Component } from 'solid-js';
import { BrowserExtensionSettings } from '../../../components/settings/BrowserExtensionSettings';

export const BrowserExtensionTab: Component = () => {
  return <BrowserExtensionSettings />;
};

export default BrowserExtensionTab;
